# Client-side configuration for ManageMC Linker Plugin

MMC_PUBLIC_KEY:
MMC_PRIVATE_KEY:

# Options: [production, demo, local]
# https://docs.managemc.com/guides/developers/api/api-environments/
MMC_ENVIRONMENT: production
